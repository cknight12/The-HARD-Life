{
  "name": "the-hard-life",
  "version": "1.0.0",
  "description": "The HARD Life - habit tracking app",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npx concurrently --kill-others-on-fail -n \"API,App\" -c \"cyan,magenta\" \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
